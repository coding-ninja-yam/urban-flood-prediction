# urban-flood-prediction
Create a modular, end-to-end pipeline in Python that:  Cleans and preprocesses the data  Engineers all rainfall and weather features  Balances classes (SMOTE)  Trains &amp; evaluates a model (XGBoost + threshold tuning)  Saves results (risk levels + model + plots)
